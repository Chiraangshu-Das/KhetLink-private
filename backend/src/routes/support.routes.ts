import { Router } from "express"; import { z } from "zod"; import { prisma } from "../../lib/prisma.js"; import { requireAuth,type AuthRequest } from "../middleware/auth.js";
const r=Router();r.use(requireAuth);r.get("/",async(req:AuthRequest,res)=>res.json({tickets:await prisma.supportTicket.findMany({where:{userId:req.userId!},orderBy:{createdAt:"desc"}})}));r.post("/",async(req:AuthRequest,res)=>{const p=z.object({subject:z.string().min(1),message:z.string().min(1)}).safeParse(req.body);if(!p.success)return res.status(400).json({error:"Subject and message are required"});res.status(201).json({ticket:await prisma.supportTicket.create({data:{...p.data,userId:req.userId!}})});});export default r;
